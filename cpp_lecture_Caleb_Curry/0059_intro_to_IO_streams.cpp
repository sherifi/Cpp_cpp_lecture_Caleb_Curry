#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <array>
#include <climits>
#include <cstdlib>
#include <ctime>

int func_59()
{
	//where we type all the data are transformed by streaming out ore in
	//after we type in keybord the data go to memory buffer
	//from memory is passed to our program
	//from input stream where we type in keybord untile we hit the enter
	//the data are stored in buffer memory
	//after we press enter the memory buffer is clearing
	//to grab data as input for our program also we can use external files
	//we use ifstream(fstream) class and create object from that class that will hold
	//our input data and this object can be called as we like for exmp. {fin} object
	//we also know that we use iostream class and cin object to get data form keybord
	//for output we use the ofstream(fstream) class and create object from that class will
	//output our data and this object can be called as we like for exmp. {fout} object
	std::cout << "Intro to IOstream" << std::endl;
	return 0;
}