#include <iostream>
#include <string>
#include <fstream>
#include <ctime>
#include <vector>
#include <array>
#include <climits>
#include <cstdlib>

template <typename T>
int func_78()
{
	//after we introduce the overloading concept for function
	//if we need to crate more solution for the same function in cpp we introduce
	// TEMPLATES
	//Using teplates we face our program with new concept the generic code
	//the same code that can be generated in different way
	void swap(T & x, T & y)
	{
		T temp;
		T b = T a;
		T a = T temp;
	}
	return 0;
}