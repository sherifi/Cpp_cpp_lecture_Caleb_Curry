#include <iostream>
#include <string>

int func_42()
{
	//for creating menu we usualy use do while loop
	//for operation in that menu we will use switch statement
	//for checking the right answer we use while loop 
	//at the right answer we will use the if statement ot break before infinit loop
	//as we guess we will store the guesses in array
	//at end we will use the for loop to print the guesses
	std::cout << "Executing the func_42" << std::endl;
	return 0;
}