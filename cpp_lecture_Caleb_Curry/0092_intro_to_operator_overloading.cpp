#include <iostream>

namespace sherifi
{
	//5 + 5
	//5-s are operant
	//+ is operator
	//overloading operator mean that we can create our custome type 
	//of operator to deal with class
	//exmple
	//let assume that we have one class called Point and the oveload that class
	//Point operator + (Point pos)
	//{
	//	Point point;
	//	point.x = x + pos.x;
	//	point.y = y + pos.y;
	//	return point;
	//}
}
int func_92()
{
	return 0;
}