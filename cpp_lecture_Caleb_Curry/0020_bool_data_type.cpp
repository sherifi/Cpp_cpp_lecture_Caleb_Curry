#include <iostream>
#include <string>

int func_20()
{
	bool pizza_is_good = true;
	bool pizza_is_good_2 = -1;
	bool pizza_is_good_3 = 1;
	bool drug_is_bad = false;
	bool drug_is_bad_2 = 0;
	//We can se bool data type to make some flags
	//For example 
	//If we crate some searching algorithm -> if the search is found we can flag it as true ore 1.
	//found = true ore found = 1
	//std::cout << std::boolalpha << pizza_is_good << std::endl;
	std::cout << pizza_is_good << std::endl;
	std::cout << pizza_is_good_2 << std::endl;
	std::cout << pizza_is_good_3 << std::endl;
	//std::cout << std::boolalpha << drug_is_bad << std::endl;
	std::cout << drug_is_bad << std::endl;
	std::cout << drug_is_bad_2 << std::endl;
	return 0;
}